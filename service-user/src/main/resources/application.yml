server:
  port: 7234

spring:
  application:
    name: service-user
  config:
    import: optional:configserver:http://localhost:3964

eureka:
  client:
    service-url:
      defaultZone: http://localhost:3743/eureka/